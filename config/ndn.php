<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | 로그인 화면 테스트 계정 표시
    |--------------------------------------------------------------------------
    |
    | true 이면 운영 콘솔 로그인 화면에 테스트 계정 목록(클릭 자동입력)이
    | 표시된다. 테스트 환경에서만 켜고, 실제 서비스 오픈 시에는 반드시 끈다.
    | .env: NDN_SHOW_TEST_LOGINS=true
    |
    */
    'show_test_logins' => (bool) env('NDN_SHOW_TEST_LOGINS', false),

    /*
    |--------------------------------------------------------------------------
    | 테스트 계정 비밀번호
    |--------------------------------------------------------------------------
    |
    | TestAccountsSeeder 가 만드는 계정들의 공통 비밀번호. 로컬 기본값은 password
    | 지만, 외부에서 접근 가능한 서버에 시드할 때는 반드시 .env 로 덮어쓴다.
    | .env: NDN_TEST_ACCOUNT_PASSWORD=...
    |
    | config 로 두는 이유: config:cache 된 환경에서는 env() 가 동작하지 않아
    | 시더가 조용히 기본값을 쓰게 되기 때문이다.
    |
    */
    'test_account_password' => env('NDN_TEST_ACCOUNT_PASSWORD', 'password'),

    /*
    |--------------------------------------------------------------------------
    | 표시 타임존 (CLAUDE.md §11)
    |--------------------------------------------------------------------------
    |
    | 날짜·시각은 UTC 로 저장하고, 화면 표시 시 이 타임존으로 변환한다.
    | 기본 Asia/Seoul(KST, UTC+9). LocalTime 헬퍼가 이 값을 사용한다.
    |
    */
    'timezone' => env('NDN_DISPLAY_TIMEZONE', 'Asia/Seoul'),

];
